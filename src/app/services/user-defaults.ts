/**
 * User Defaults
 * the default settings keys and values for each user in the DB
 */
export class UserDefaults {
  fontColor: string = 'blue';
  fontFamily: string = 'Roboto';
  fontSize: number = 14;
  defaultView: string = 'week';
}
